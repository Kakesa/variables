// Les objets sont des structures de données qui permettent de regrouper des valeurs sous un même nom.

const user = {
    name: "John Doe",
    age: 30,
    email: "espoir@gmail.com",
}

// Accéder aux propriétés d'un objet
console.log(user.name); // "John Doe"
console.log(user.age); // 30